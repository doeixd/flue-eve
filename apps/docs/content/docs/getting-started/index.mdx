---
title: Getting Started
description: Pick the fastest path for a new app, an Eve migration, or a client integration
---

# Getting Started

Pick the path that matches the code you already have. The browser-facing
contract is Eve-compatible; the server runtime is Flue.

## Prerequisites

- Node.js 22.19.0 or later for Flue.
- pnpm 9.x for this repository, or your package manager in an app project.
- An LLM provider key only when you want real model calls. Mock admission works
  without secrets.

## Choose your path

| Path | Use it when | Result |
|------|-------------|--------|
| [New project](/docs/getting-started/new-project) | You want a Vite + React app | Same-origin `/eve/v1/*`, React UI, Flue agent |
| [From Eve](/docs/getting-started/from-eve) | You already have Eve code | Aliased client/hook imports and Flue-backed server |
| [Frontend only](/docs/getting-started/frontend-only) | The server already exists | Browser code talks to a remote compat server |
| [React](/docs/getting-started/react) | You are building a chat UI | `useEveAgent` with streaming state |
| [Client](/docs/getting-started/client) | You are writing scripts or tests | `Client` and `ClientSession` over HTTP |
| [Cloudflare Worker](/docs/getting-started/cloudflare-worker) | You want edge deployment | Worker app with KV/DO-backed persistence |

## What the quickstarts produce

- A browser UI that talks to same-origin `/eve/v1/*` routes during development.
- A compat server mounted in the Flue app, not in Vite middleware.
- Eve-shaped session responses and NDJSON stream events.
- Mock admission by default, so setup works before provider keys are configured.
- A path to real Flue execution by setting `FLUE_BASE_URL=http://127.0.0.1:3583`.

## How it works

```text
React hook or TypeScript client
  -> /eve/v1/session, /stream
  -> compat-server journal + mapper
  -> Flue agent instance
```

Vite handles integration: proxying, generated sidecar files, aliases, and health
checks. The compat server handles runtime translation: session tokens, event
mapping, NDJSON, auth, and journal replay.

## Need help?

- [Troubleshooting](/docs/guides/troubleshooting) — common issues and fixes
- [Eve and Flue](/docs/concepts/eve-and-flue) — which project owns which layer
- [Architecture](/docs/concepts/architecture) — the data flow and invariants
- [Migration tiers](/docs/concepts/migration-tiers) — what can be automated
