---
title: Authoring Instructions
description: Write agent instructions
---

# Authoring Instructions

Agent instructions are set via `createAgent` in Flue:

```ts
createAgent({
  instructions: 'You are a helpful assistant.',
});
```

Eve-style `agent/instructions.md` file support is planned.
